- name: intsall nginx
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: intsall
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Start service nginx, if not started
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes